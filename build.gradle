plugins {
    id 'java'
}

group = 'com.simpleAppiumTest'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.testng:testng:7.9.0'
    implementation 'io.appium:java-client:9.1.0'
    implementation 'org.seleniumhq.selenium:selenium-java:4.18.1'
    implementation 'org.apache.logging.log4j:log4j-core:2.23.0'
}

test {
    useTestNG() {
        if (project.hasProperty('wikipediaTest')) {
            suites './src/test/resources/suites/wikipediaTest.xml'
        }
        if (project.hasProperty('wikipediaSingleTest')) {
            suites './src/test/resources/suites/wikipediaSingleTest.xml'
        }
        if (project.hasProperty('vintedTest')) {
            suites './src/test/resources/suites/vintedTest.xml'
        }
    }
}